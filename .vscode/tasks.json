{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build hello world",
            "command": "/usr/bin/clang",
            "args": [
                "-g",
                "${workspaceFolder}/main.c",
                "-o",
                "${workspaceFolder}/chip8.out",
                "-lSDL2"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}